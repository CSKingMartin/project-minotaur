export const FullExample = (props) => {
	const {
		children
	} = props;

	return (
		<div>Full Example</div>
	);
};

FullExample.propTypes = {
	children: PropTypes.node
};

export default FullExample;